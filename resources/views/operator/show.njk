{% extends 'master' %}

{% block content %}
<head>
<title>TABEL OPERATOR</title>
</head>
<BR>
	<BR>
	<div class="col-md-8 col-md-offset-2">
	</br>
</br>
		<table>
			<tr>
				<td style="padding-right: 10px;">
    				{{ form.open({ route: 'operatorIUD.edit', params: {id: operator.id} }) }}
    					{{ form.button('Update','', { class: 'btn btn-success '}) }}
    				{{ form.close() }}

    			</td>

    			<td style="padding-right: 20px;">
    				{{ form.open({ route: 'operatorIUD.destroy', params: {id: operator.id} }) }}
    					{{csrfField}}
    					{{ form.button('Delete','', { class: 'btn btn-danger '}) }}

    				{{ form.close() }}

    			</td>
					<td style="padding-right: 30px;">
    				{{ form.open({ route: 'operatorIUD.edit', params: {id: operator.id} }) }}
    					{{csrfField}}
    					{{ form.button('Edit','', { class: 'btn btn-success '}) }}

    				{{ form.close() }}
    			</td>
    		</tr>
		</table>
		<br>
		<br>
		<table class="table table-striped">
			<tbody>
				<tr>
					<th class="col-md-2">id</td>
					<td class="col-md-6">{{ operator.id }}</td>
				</tr>
				<tr>
					<th class="col-md-2">Create Date</th>
					<td class="col-md-6">{{ operator.created_at }}</td>
				</tr>
				<tr>
					<th class="col-md-2">Update Date</th>
					<td class="col-md-6">{{ operator.updated_at }}</td>
				</tr>
				<tr>
					<th class="col-md-2">Nama operator</th>
					<td class="col-md-6">{{ operator.name }}</td>
				</tr>
				<tr>
					<th class="col-md-2">Number</th>
					<td class="col-md-6">{{ operator.number }}</td>
				</tr>



			</tbody>
		</table>
	</div>

{% endblock %}
