{% extends 'master' %}

{% block content %}
<head>
<title>TABEL DRIVER</title>
</head>
<div class="col-md-12 col-md-offset-0">
<h3><b>TABEL DRIVER</b></h3>
</br>
</br>

        {{form.open({route:'driverIUD.create'})}}
            {{form.button('Create','',{class:'btn btn-success'})}}
        {{form.close()}}

        <table class="table table-striped">
        <thead>
          <tr>
            <th>Name</th>
            <th>Create Date</th>
		        <th>Update Date</th>
		        <th>SIM</th>
					  <th>status</th>
          </tr>
        <thead/>
        <tbody>

        {% for driver in drivers %}
          <tr>
            <td>{{driver.name}}</td>
            <td>{{driver.created_at }}</td>
		        <td>{{ driver.updated_at }}</td>
		        <td>{{ driver.SIM}}</td>
		        <td>{{ driver.status }}</td>
            <th><a href="driverIUD/{{driver.id}}">View</a></th>
          <tr>
          {%endfor%}
          </tbody>

{% endblock %}
